# src/params.py

# Base resolution reference (used for scaling or validation)
resolution = {
    "width": 1920,
    "height": 1080
}

# OCR target region defined by upper-left and lower-right corners
ocr_region = {
    "upper_left": {"x": 1035, "y": 170},
    "lower_right": {"x": 1700, "y": 900}
}